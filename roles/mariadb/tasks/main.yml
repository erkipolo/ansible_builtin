---
- name: Install MariaDB
  ansible.builtin.apt:
    name:
      - mariadb-server
    state: present

- name: Arrancamos y Habilitamos en el arranque MariaDB
  ansible.builtin.service:
    name: mysql
    state: started
    enabled: yes
